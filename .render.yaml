services:
  - type: web
    name: my-anime-bot
    runtime: python
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python bot.py"
    envVars:
      - key: BOT_TOKEN
        value: "YOUR_BOT_TOKEN_HERE"
